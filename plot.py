import turtle

# Set up screen
screen = turtle.Screen()
screen.title("Mona Lisa with Turtle")

# Add Mona Lisa image (must be in GIF format)
screen.bgpic("monalisa.gif")  # Make sure the file is in the same directory

# Keep the window open
turtle.done()
